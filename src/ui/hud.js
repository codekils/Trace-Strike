import { drawCrosshair } from './crosshair.js';
export class HUD { constructor(element) { this.element = element; } update(player, weapon, enemies, sector, wave) { const hostiles = String(enemies.filter(enemy => !enemy.dead).length).padStart(2, '0'); this.element.innerHTML = `<div class="hud-stack"><span class="hud-green">HP ${String(player.health).padStart(3, '0')}</span><span>AMMO <b class="hud-green">${weapon.ammo} / ${weapon.reserve}</b></span><span>ENEMIES ${hostiles}</span><span class="hud-green">SECTOR ${sector}</span><span>WAVE ${String(wave).padStart(2, '0')}</span></div>`; } drawCrosshair(ctx, canvas, active) { drawCrosshair(ctx, canvas, active); } }