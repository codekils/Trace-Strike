import { drawCrosshair } from './crosshair.js';
export class HUD { constructor(element) { this.element = element; } update(player, weapon, enemies, sector, wave) { this.element.innerHTML = `<div class="hud-line"><span>HP ${String(player.health).padStart(3, '0')}</span><span>SECTOR ${sector}</span></div><div class="hud-line"><span>WAVE ${String(wave).padStart(2, '0')}</span><span>HOSTILES ${String(enemies.filter(enemy => !enemy.dead).length).padStart(2, '0')}</span></div><div class="hud-ammo"><small>AMMO</small><strong>${weapon.ammo} / ${weapon.reserve}</strong></div>`; } drawCrosshair(ctx, canvas, active) { drawCrosshair(ctx, canvas, active); } }