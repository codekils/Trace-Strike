export function drawCrosshair(ctx, canvas, active = false) { const x = canvas.width / 2; const y = canvas.height / 2; const size = active ? 13 : 9; ctx.strokeStyle = active ? '#ffcf7a' : '#e9f0e8'; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(x - size, y); ctx.lineTo(x - 3, y); ctx.moveTo(x + 3, y); ctx.lineTo(x + size, y); ctx.moveTo(x, y - size); ctx.lineTo(x, y - 3); ctx.moveTo(x, y + 3); ctx.lineTo(x, y + size); ctx.stroke(); }