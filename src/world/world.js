import { MAP, SPAWN_POINTS } from './map.js';
export class World { constructor() { this.map = MAP; this.width = MAP[0].length; this.height = MAP.length; this.enemies = []; this.spawnPoints = SPAWN_POINTS; this.walls = []; for (let y = 0; y < this.height; y++) for (let x = 0; x < this.width; x++) if (MAP[y][x] === '#') this.walls.push({ x, y }); } isWall(x, y) { const cell = this.map[Math.floor(y)]?.[Math.floor(x)]; return cell === '#'; } getSpawnPositions(count, playerPosition) { const valid = this.spawnPoints.filter(point => !this.isWall(point.x, point.y) && Math.hypot(point.x - playerPosition.x, point.y - playerPosition.y) > 2.5); const positions = []; for (let index = 0; index < count; index++) positions.push({ ...valid[index % valid.length] }); return positions; } }